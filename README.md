# MNIST-Digit-Classification
A Streamlit application that compares the performance of a Perceptron, an Artificial Neural Network (ANN), and a Convolutional Neural Network (CNN) on the MNIST handwritten digit dataset.
